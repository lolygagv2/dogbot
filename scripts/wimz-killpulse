#!/bin/bash
# Last userspace code before kernel halt.
# Pulses GPIO26 HIGH for 1 second to trigger Pololu OFF and cut power.
# Installed at /lib/systemd/system-shutdown/wimz-killpulse (mode 0755, root).
/usr/bin/gpioset --mode=time --sec=1 gpiochip0 26=1
